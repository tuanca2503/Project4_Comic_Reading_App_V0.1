// use to pass data from modal to screen
import 'package:project4/screens/notification/bottom_sheet_option.dart';

class NotificationPassData {
  String? id;
  NotificationOption? option;

  NotificationPassData(this.id, this.option);
}